# TEMPLATE ONLY — the app does NOT read this root file.
# Real values live in:
#   apps/web/.env.local      (web: Firebase web config, Maps key)
#   services/api/.env        (server: Gemini, service account, Supabase)
# Copy fresh values from .env.example when needed.
